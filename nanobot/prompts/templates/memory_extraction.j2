你是一个记忆提取器。请从下面会话中提取长期有价值的信息，并按 JSON 输出。

输出要求:
1. 只输出 JSON，不要 markdown。
2. 顶层结构:
{
  "memories": [
    {
      "category": "profile|preferences|entities|events|cases|patterns",
      "abstract": "一句话摘要（L0）",
      "overview": "结构化概要（L1）",
      "content": "完整内容（L2）"
    }
  ]
}
3. 如果没有可提取内容，返回 {"memories": []}。
4. 输出语言: {{ output_language }}。

会话标识: {{ session_key }}

会话内容:
{{ messages }}
